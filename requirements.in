# Human-editable dependency constraints.
# Compile to requirements.txt with pinned versions + hashes:
#   uv pip compile --generate-hashes --exclude-newer "7 days" --python-version 3.12 requirements.in -o requirements.txt
httpx
mcp[cli]
zenml~=0.93.0
setuptools
requests>=2.32.0
